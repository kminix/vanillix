name: Deploy-Self-Hosted

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd /var/www/minixmanor
          git pull
          composer install --no-dev
          php artisan optimize
